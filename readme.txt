Object oriented library for HTML elements. See the example below


$obj=new textbox();

$obj->id='name';            // assigning ID
$obj->get_textbox();  

This will include all basic functions in default and user can also change it's default functionality.

      